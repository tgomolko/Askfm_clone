<% if user_signed_in? and current_user.id != @user.id %>
  <% if  current_user.following.any? and current_user.following?(@user)%>
    <%= render 'unfollow'%>
  <% else%>
    <%= render 'follow'%>
  <% end %>
<% end %>
